
local function runmsg
    
    local data = load_data_config.moderation.data
    
     if datatostringmsg.to.id'settings''antilink' == 'yes' then
      
    
if not is_mod(msg) then
    
    
chat_del_user'chat#id'..msg.to.id, 'user#id'..msg.from.id, ok_cb, true
    local msgads = 'ForbiddenAdText'
   local receiver = msg.to.id
    send_large_msg'chat#id'..receiver, msg.."\n", ok_cb, false
	
      end
   end
end
    
return {patterns = {
"https://telegram.me/joinchat/",
"HTTPS://TELlEGRAM.ME/JOINCHAT",
"telegram.me/joinchat/",
"TELEGRAM.ME/JOINCHAT/",
"Https://",
"www:",
}, run = run}
